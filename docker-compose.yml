version: "3"

services:
  cli:
    image: ${LAMMPS_CLI_IMAGE}
    working_dir: /code/experiment-lammps
    command: ${INNER_SHELL}
    stdin_open: true
    tty: true
    network_mode: host
    volumes:
      - .:/code/experiment-lammps
